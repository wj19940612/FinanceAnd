<com.sbai.finance.view.CustomSwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/swipeRefreshLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background"
        android:gravity="center"
        android:orientation="vertical">

        <ListView
            android:divider="@null"
            android:id="@android:id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:dividerHeight="0.5dp"
            android:visibility="visible"/>

        <com.sbai.finance.view.ListEmptyView
            android:id="@+id/listEmptyView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:titleImage="@drawable/common_ic_no_audio_collect"
            app:goingBg="@drawable/bg_annals_materials_4"/>
    </LinearLayout>

</com.sbai.finance.view.CustomSwipeRefreshLayout>
